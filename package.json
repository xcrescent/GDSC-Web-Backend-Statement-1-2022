{
  "name": "to-do-list",
  "version": "1.0.0",
  "description": "Make a to-do list web app that stores the users data at your server so that users can login from any device and access that list. ",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Utkarsh Singh",
  "license": "ISC",
  "devDependencies": {
    "requirement": "^0.0.7",
    "requirements": "^1.4.1"
  },
  "dependencies": {
    "nodemon": "^2.0.19",
    "express":"^4.18.1",
    "mongodb": "^4.8.0",
    "mysql": "2.18.1"
  }
}
